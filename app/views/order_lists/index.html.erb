<h1>Listing order_lists</h1>

<%= will_paginate %>

<table>
  <tr>
    <th>User</th>
    <th>Start date</th>
    <th>End date</th>
    <th>Due date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @order_lists.each do |order_list| %>
  <tr>
    <td><%= order_list.user_id %></td>
    <td><%= order_list.start_date %></td>
    <td><%= order_list.end_date %></td>
    <td><%= order_list.due_date %></td>
    <td><%= link_to 'Show', order_list %></td>
    <td><%= link_to 'Edit', edit_order_list_path(order_list) %></td>
    <td><%= link_to 'Destroy', order_list, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<%= will_paginate %>

<br />

<%= link_to 'New Order list', new_order_list_path %>
