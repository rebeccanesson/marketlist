<%= form_for(@product_family) do |f| %>
  <% if @product_family.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_family.errors.count, "error") %> prohibited this product_family from being saved:</h2>

      <ul>
      <% @product_family.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
